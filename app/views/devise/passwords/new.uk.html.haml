- content_for :title, 'Відновити пароль'

.thin-page
  %h1.long-title= yield :title

  %p Напиши свою електронну скриньку і ми надішлемо тобі інструкції щодо відновлення паролю

  = form_for(resource, as: resource_name, url: password_path(resource_name), html: { method: :post }) do |f|
    = devise_error_messages!
    .form-group
      = f.label :email
      = f.email_field :email, autofocus: true, class: "form-control", required: "required"

    = f.submit "Відновити пароль", class: "btn btn-primary btn-wide"

  %ul.form-secondary.nav.nav-pills
    %li= link_to 'Повернутись до авторизації', new_user_session_path, class: "btn-xs"
