- content_for :title, 'Зареєструйся'

.thin-page.signup-page
  .signup-intro
    %h1.long-title Зареєструйся, щоб більше знати про роботу парламенту

    %p
      Приєднуйся до спільноти, яка розбудовує цей сайт.
      %span.hidden-xs
        Додавай нові Політики, редагуй та покращуй описи голосувань, а також підписуйся на оновлення Політик, які тебе цікавлять.

  = simple_form_for(resource, as: resource_name, url: registration_path(resource_name), html: {class: "signup-form"}) do |f|
    - if f.error(:email) =~ /already been taken/
      .alert.alert-danger
        %em= resource.email
        has already been registered.
        Would you like to #{link_to "sign in", new_user_session_path(:'user[email]' => resource.email)}?
    - else
      = f.error_notification

    .form-group= f.input :email, as: :email, autofocus: true, hint: "Введіть свою електронну адресу", hint_html: {class: 'small hint-block-intime'}
    .form-group= f.input :name, as: :string, label: "Ім'я користувача", hint: "Твоє ім'я, яке ти обрав/ла для сайту, буде оприлюднене на сторінках Політики чи Голосування, які ти редагував/ла чи створював/ла", hint_html: {class: 'small hint-block-intime'}
    .form-group= f.input :password, label: "Пароль" ,hint: "Мінімум 8 символів", hint_html: {class: 'small hint-block-intime'}

    = f.button :submit, "Зареєструватися", class: 'btn-primary btn-wide submit-signup'

    %p.form-secondary.small.help-block
      %strong Приватність:
      Жодна інформація, яку ти тут надаєш, не буде надана стороннім особам.
